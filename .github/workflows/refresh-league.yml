name: Refresh League Data

on:
  workflow_dispatch:  # manual trigger from GitHub UI

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Call backend /refresh
        run: |
          curl -X POST "${{ secrets.LSL_BACKEND_REFRESH_URL }}"
